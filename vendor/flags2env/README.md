# flags2env vendor

This directory vendors the native parser from
https://github.com/ORESoftware/flags-2-env so the `lmxd` broker can parse
`.cli-flags.toml` without requiring a separate shared library at runtime.
